package app.udrinkidrive.feed2us.com.customer;

import android.util.Log;

//import com.onesignal.OSNotification;
//import com.onesignal.OneSignal;

import org.json.JSONObject;

import app.udrinkidrive.feed2us.com.customer.activity.ActivityDriver;

/**
 * Created by sarinsuriyakoon on 5/18/18.
 */

class NotificationReceivedHandler{

//class NotificationReceivedHandler implements OneSignal.NotificationReceivedHandler {
//    @Override
//    public void notificationReceived(OSNotification notification) {
//        JSONObject data = notification.payload.additionalData;
//        Log.d("Json Noti",data.toString());
//        ActivityDriver driver = new ActivityDriver();
//        driver.getDriverInfo();
////        String customKey;
////
////        if (data != null) {
////            customKey = data.optString("customkey", null);
////            if (customKey != null)
////                Log.i("OneSignalExample", "customkey set with value: " + customKey);
////        }
//    }
}
